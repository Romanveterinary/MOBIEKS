name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    # СТАВИМО ЖОРСТКИЙ ТАЙМЕР! Ніяких більше 6 годин.
    timeout-minutes: 35 

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Tools
      run: |
        pip install flet[all]
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Build APK (Diagnostic Mode)
      env:
        CI: true
        PYTHONUNBUFFERED: "1" # Примусово виводить усі логи в реальному часі!
        FLET_DEBUG: "true"    # Вмикаємо режим діагностики
      run: |
        yes | flutter doctor --android-licenses
        yes | flet build apk .

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: VetAI-Pro-APK
        path: |
          build/apk/*.apk
          build/flutter/build/app/outputs/flutter-apk/*.apk
          build/app/outputs/flutter-apk/*.apk
        if-no-files-found: warn
