- name: Build APK (The Simple Way)
      run: |
        # 1. Приймаємо ліцензії (це важливо)
        yes | flutter doctor --android-licenses
        
        # 2. Тільки сама команда. Жодних --project-name, жодних --verbose.
        # Flet автоматично знайде main.py в папці.
        flet build apk
